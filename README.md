# FlowMail 🚀

A **Superhuman-style Gmail & Google Calendar workflow app** built with Next.js, Postgres, and Corsair.

## What makes it different

- **AI-powered priority filtering** — every email gets classified as 🔴 High / 🟡 Medium / ⚫ Low priority automatically using GPT-4o-mini
- **Keyboard-first** — navigate your entire inbox without touching the mouse (J/K, C, E, R, G+I/S/T/C, ⌘K)
- **AI Agent chat** — "Send a calendar invite to friend@corsair.dev at 9 AM next Thursday. Send him an email too saying I look forward to our meeting."
- **Real-time webhooks** via Corsair — no polling, emails and calendar events arrive instantly
- **Lightning-fast local search** — searches the local Postgres cache first before hitting the Gmail API
- **Smart replies** — one-click reply suggestions generated by AI
- **Unified calendar + email** — create events and automatically send email invites in one click

## Tech stack

- **Next.js 16** (App Router, Server Components, API routes)
- **PostgreSQL + Prisma** (local cache, chat history, user data)
- **Corsair** (Gmail API, Google Calendar API, webhooks, MCP agent)
- **OpenAI GPT-4o-mini** (priority classification, smart replies, agent chat)
- **NextAuth v5** (Google OAuth with Gmail + Calendar scopes)

## Corsair features used

- ✅ Gmail API via Corsair (list, get, send, modify, search)
- ✅ Google Calendar API via Corsair (list, create, update, delete events)
- ✅ Corsair Webhooks (real-time Gmail + Calendar push notifications)
- ✅ Corsair MCP Agent (natural language → send email + create events)
- ✅ Corsair Search API (advanced Gmail search with operator support)

## Bonus tasks attempted

- ✅ **Agent chat** — AI assistant with Corsair MCP integration
- ✅ **Realtime webhooks** — `/api/webhooks/corsair` receives Corsair push events
- ✅ **AI priority filtering** — automatic email prioritization
- ✅ **Keyboard shortcuts** — full keyboard navigation + command palette
- ✅ **Corsair search API** — advanced search with Gmail operators
- ✅ **Local search cache** — fast Postgres search before API fallback

## Setup

### 1. Clone and install

```bash
git clone <your-repo>
cd flowmail
npm install
```

### 2. Set up environment variables

Copy `.env.local.example` to `.env.local` and fill in:

```
DATABASE_URL=postgresql://...
NEXTAUTH_SECRET=your-secret
GOOGLE_CLIENT_ID=...
GOOGLE_CLIENT_SECRET=...
OPENAI_API_KEY=...
CORSAIR_API_KEY=...
CORSAIR_BASE_URL=https://api.corsair.dev
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

### 3. Set up the database

```bash
npx prisma generate
npx prisma db push
```

### 4. Google OAuth setup

In Google Cloud Console:
1. Create OAuth 2.0 credentials
2. Add authorized redirect URI: `http://localhost:3000/api/auth/callback/google`
3. Enable Gmail API and Google Calendar API
4. Add scopes: `gmail.readonly`, `gmail.send`, `gmail.modify`, `calendar`, `calendar.events`

### 5. Run

```bash
npm run dev
```

## Keyboard shortcuts

| Key | Action |
|-----|--------|
| `C` | Compose new email |
| `J` / `↓` | Next email |
| `K` / `↑` | Previous email |
| `Enter` | Open selected email |
| `E` | Archive email |
| `S` | Star/unstar |
| `R` | Reply |
| `/` | Search |
| `G I` | Go to Inbox |
| `G S` | Go to Sent |
| `G T` | Go to Starred |
| `G C` | Go to Calendar |
| `N` | New calendar event |
| `⌘K` | Command palette / AI Agent |
| `?` | Show help |
| `Esc` | Close panel |

## Agent examples

> "Send a calendar invite to friend@corsair.dev at 9 AM next Thursday and send them an email saying I look forward to our meeting"

> "Summarize my unread emails"

> "Schedule a 30-minute standup with team@company.com tomorrow at 10am"

> "Search for emails from my boss this week"
